# Reuses the same .venv as steps 1-3.
# Pinning explicitly only the dependencies that this step actually imports:
# - identity seed generation imports vllm (server-only) via the step 2 client
# - augment.py is pure stdlib + json
#
# Hard-pinned versions match the working stack already validated on the A100
# server (CUDA 12.4 driver / cu126 forward-compat).
vllm==0.9.2
transformers==4.52.4
tokenizers<0.22
pyarrow>=18.0,<20.0
