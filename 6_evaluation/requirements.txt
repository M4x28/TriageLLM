# Step 6 evaluation requirements. Reuses .venv-sft from step 5 (transformers
# 4.57, peft 0.19, torch 2.12). Datasets is already installed; we only need
# explicit pins to keep the env reproducible.
torch>=2.4,<3.0
transformers>=4.55,<5.0
peft>=0.14,<1.0
accelerate>=1.2,<2.0
datasets>=3.0,<4.0
huggingface_hub>=0.26,<1.0
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0
tqdm>=4.67,<5.0

# Phase 2 behavioral evaluation (Bloom, on the inspect_ai substrate).
# Installed in a dedicated venv (.venv-bloom) — it pulls inspect_ai + an openai
# client and is kept apart from the transformers/vLLM serving stack. The served
# target + auditor/judge models run under the separate vLLM env.
petri-bloom>=0.2,<1.0     # provides the `bloom` CLI + petri_bloom/bloom_audit inspect task
# inspect_ai is pulled transitively by petri-bloom (spike used 0.3.233).

